export function {{basename outpath}}() {}

if (import.meta.vitest) {
  const { describe, test } = import.meta.vitest;

  describe("{{dirname outpath}}/{{basename outpath}}", () => {
    test.skip("Should be tested");
  });
}
